'use strict';

describe('Mechanics Booking application', function () {

    beforeEach(module('MechanicBookings'));

    //it('has an app object initialised', inject(function () {
    //    expect(app).toBeDefined();
    //}));

    it('has an app object initialised', function () {
        expect(app).toBeDefined();
    });

});
